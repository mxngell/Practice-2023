Список введеных команд:
=======================================================================

$ mkdir Nikaev
$ cd Nikaev
$ mkdir Storage
$ cd Storage
$ touch host.txt
$ touch osd.txtrm 
$ touch san.txt
$ echo "Хост-система или главный компьютер: вычислительная платформа (оборудование, программно-аппаратные средства и программное обеспечение), обеспечивающая работу приложений и баз данных." >  host.txt
$ echo "Объектно-ориентированное хранилище (OSD-устройство) представляет собой способ хранить файловые данные в форме объектов, используя в качестве основы вместо имен и расположения содержимое и другие признаки этих файловых данных." > osd.txt
$ echo "SAN представляет собой высокоскоростную выделенную сеть серверов и совместно используемых устройств хранения данных." > san.txt
$ cd ..
$ mkdir Storage-backup
$ cd Storage-backup
$ tar --create --bzip2 --file=Incr-Storage-backup.tar.bz2  --listed-incremental=Incr-Storage-backup.snar ../Storage
$ cd ..
$ cd Storage
$ echo "Для хранения данных в OSD используется одноуровневое адресное пространство. Следовательно, в нем нет иерархии каталогов и файлов, в результате чего OSD-система позволяет сохранять громадное количество объектов." >> osd.txt
$ cd ..
$ cd Storage-backup
$ tar --create --bzip2 --file=Incr-Storage-backup.tar.bz2 ../Storage